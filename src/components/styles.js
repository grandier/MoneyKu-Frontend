import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  image: {
    width: 50,
    height: 50,
    borderRadius: 10,
    borderWidth: 1,
    marginRight: 5,
    borderColor: "white",
    alignItems: "flex-end",
  },
});
export default styles;
